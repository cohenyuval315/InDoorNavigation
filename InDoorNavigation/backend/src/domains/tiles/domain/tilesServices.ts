import path from 'path';
// import fs from 'fs-extra'; // Use fs-extra for easier file operations

// const tileDir = path.join(__dirname, 'tiles'); // Directory where tiles are stored
// const tilePath = path.join(tileDir, floor, `${z}/${x}/${y}.pbf`);

export const getTile = async (floor:string, z:string, x:string, y:string) => {
    // const tilePath = path.join(__dirname, 'tiles', `${floor}.mbtiles`);
    
    return "";
}   

export const getBuildingTile = async (floor:string, z:string, x:string, y:string) => {
    // const tilePath = path.join(__dirname, 'tiles', `${floor}.mbtiles`);
    return "";
    
    
} 

export const getGlobalTile = async (filename:string) => {
    // const tilePath = path.join(__dirname, 'tiles', `${floor}.mbtiles`);
    
    return "";
} 



