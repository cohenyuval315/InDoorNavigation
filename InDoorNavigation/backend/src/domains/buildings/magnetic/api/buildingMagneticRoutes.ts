import {Router, Request, Response, NextFunction } from 'express';
import * as buildingMagneticController from './buildingMagneticController.js';

const magneticRouter = Router();

export default magneticRouter;