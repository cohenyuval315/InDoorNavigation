import { View } from "react-native";

const GlobalMapUserOverlay = ({offsetFunction}) => {
    return (
        <View style={{
            position:"absolute",
            zIndex:22,
        }}>

        </View>
    )
}

export default GlobalMapUserOverlay;