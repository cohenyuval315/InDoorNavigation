import { useMemo } from "react";
import MapOverlay from "../../../../../layouts/map-overlay";

const RouteOverlay = ({points}) => {
    const routes = useMemo(() => {
        return (
            <>
            </>
        )
    },[]);
    return (
        <MapOverlay>
            
        </MapOverlay>
    )
}

export default RouteOverlay;